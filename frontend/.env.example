# Optional. If unset, the frontend runs entirely on local mock data
# (lib/mockSignals.js) — no backend, no API keys required to review
# or run this frontend layer.
#
# If you want to connect it to the real Express backend built earlier,
# set this to where that service is running, e.g.:
# NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
